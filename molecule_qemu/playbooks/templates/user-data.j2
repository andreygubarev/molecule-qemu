#cloud-config

ssh_pwauth: false
users:
  - name: ansible
    gecos: Ansible User
    groups: admin,users,sudo,wheel
    shell: /bin/bash
    lock_passwd: true
    ssh_authorized_keys:
      - {{ ssh_keypair.public_key }}
